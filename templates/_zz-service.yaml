apiVersion: v1
kind: Service
metadata:
  name: {{ include "hadoop-basic.fullname" . }}
  labels:
    {{- include "hadoop-basic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hadoop-basic.selectorLabels" . | nindent 4 }}
